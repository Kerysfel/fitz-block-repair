# textblock-clustering

Minimal text span clustering (BFS) and optional watermark filtering for PyMuPDF.

## Install

```bash
pip install -e .
```

## Example

```bash
python examples/demo.py path/to/file.pdf
```

```python
from textblock_clustering import TextClustering

# Use TextClustering on a fitz.Page instance
```
